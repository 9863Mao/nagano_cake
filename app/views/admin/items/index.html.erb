<h2>商品一覧</h2>

<% @items.each do |item| %>
  <%= item.id %>
  <%= link_to admin_item_path(item.id) do %>
    <%= item.name %>
  <% end %>
  <%= item.price %>
  <%= item.genre.name %>
  <% if item.is_active? %>
    <p>販売中</p>
  <% else %>
    <p>販売停止中</p>
  <% end %>
<% end %>
